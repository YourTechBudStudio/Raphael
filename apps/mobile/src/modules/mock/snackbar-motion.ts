/** THROWAWAY MOCK. In and out inside the app's 150–250 ms band: a short rise with a crisp settle, no spring. */
export const SNACKBAR_ENTER_MS = 200;
export const SNACKBAR_EXIT_MS = 150;
/** Height of the bar, for anything that has to make room for it. */
export const SNACKBAR_HEIGHT = 56;
