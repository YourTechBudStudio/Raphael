import { MockSearchScreen } from '../modules/search/mock/MockSearchScreen';

/** THROWAWAY: presentation mock for story #6. Delete before merging. */
export default function MockSearchRoute() {
  return <MockSearchScreen />;
}
